// import {Inter} from 'next/font/google'

// const inter = Inter({subsets: ['latin']})

export default function Home() {
  return (
      <main className={'h-screen w-full flex flex-col justify-center sm:items-center bg-amber-50 sm:bg-amber-100 md:bg-amber-500 lg:bg-amber-800'}>
      </main>
  )
}
